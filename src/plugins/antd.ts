import {App} from "vue";
import Antd from 'ant-design-vue';
import 'ant-design-vue/dist/reset.css';

export default {
    install: (app: App) => {
        // 注册到vue全局
        // app.use(Antd);
    },
};